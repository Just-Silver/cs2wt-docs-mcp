# 协作约定

本文件定义本项目的协作模式，避免每次会话重复沟通。

## 语言

- 与用户交流、撰写文档一律使用**简体中文**。
- Git commit 信息使用**中文**。

## 决策边界

用户**不熟悉 Python 生态**，因此分工如下：

- **方向（用户决策）**：技术栈、依赖取舍、架构取向、对外接口 / 工具集、产品定位、仓库命名与可见性、远程仓库的创建与删除。
- **细节（助手自主）**：规格、设计、实现计划、代码结构、命名、测试、错误处理、分支策略。
- 原则：**方向问用户，细节自己定**。出现新的方向性问题时，用简短选项征询用户，不要长篇论证。

## 工作流程

1. 功能开发前走 brainstorming 流程；architectural 任务需将 spec 写入 `docs/superpowers/specs/`。
2. spec 与实现计划（plan）由助手**自审**后落盘并提交，不要求用户逐节审批。
3. 开发**一律使用子代理**执行（每个任务派发独立子代理，任务间由助手审查）。
4. **是否新建分支、分支如何命名，均由助手自行决策**（默认 `feat/<主题>`，完成后合并回 `main`）。
5. 远程仓库操作（创建 / 删除 / 改名）由**用户**决定，助手不擅自执行。

## 技术背景

- 项目：Counter-Strike 2 Workshop Tools 文档的离线索引工具（CLI + MCP）。
- 语言：Python ≥ 3.10。
- 抓取源：Valve Developer Community（受 Anubis 反爬保护，需 SHA-256 PoW 求解）。
- 检索：SQLite FTS5 全文索引。
- 新引入的依赖需登记到 `pyproject.toml`。